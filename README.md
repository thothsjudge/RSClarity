RSClarity

Free Auto Clicker – RSClarity

Click RSClarity.exe, then click View Raw to download it, or copy and paste this link into your browser:
https://thothsjudge.github.io/RSClarity/

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
NOTICE

Because RSClarity is an unsigned tool that simulates mouse input, Windows Defender, SmartScreen, or other antivirus/firewall products may flag it as suspicious, especially the first time you run it.

If you downloaded RSClarity from my official link:
https://thothsjudge.github.io/RSClarity/

UAC Prompt — When Windows asks if you want to allow this app to make changes, click Yes to enable global hotkeys and ensure proper functionality.
SmartScreen ("Windows protected your PC") — Click More info → Run anyway to start RSClarity.
Antivirus / Firewall Blocks — You may need to allow RSClarity and/or add it as an exception.

Never disable your security software globally just for RSClarity. Adjust only the settings you need.

GLOBAL HOTKEYS (F4–F6) ONLY WORK WHEN RUN AS ADMINISTRATOR.
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
RSClarity

RSClarity is a desktop automation tool that simulates repeatable mouse actions with human-like timing. Configure CPS ranges, run duration, and action limits; record your own CPS; target a custom screen area; and use the built-in calculator. It also includes Anti-Detection, jitter, configurable click rates, and global hotkeys for precise, flexible control. It is designed for stress-testing, repetitive workflows, and input automation on Windows.

Key Features
CPS overrides (per minute, per second, and per millisecond) with CPS range priority logic.
Run Duration timer (H:M) with a pause-aware countdown.
RSClarity Limit counter that automatically stops after a chosen number of actions.
Location Area capture overlay for targeting a custom region of the screen.
Optional Anti-Detection jitter and micro-delays for more natural-looking input.
Built-in CPS recorder and mini calculator.

GLOBAL HOTKEYS (F4–F6) ONLY WORK WHEN RUN AS ADMINISTRATOR.
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Requirements
Windows 10 or later.
Quick Use
Set your CPS or CPS Range (5–11 by default).
Optionally set a Run Duration and/or RSClarity Limit.
(Optional) Use Record CPS to measure your natural clicking speed.
Optionally set a Location Area and enable it.
Click Start or press F4. Use F6 to pause/resume and F5 to stop.
Safety & Responsibility

Only use RSClarity where automation is permitted. Do not use it to violate terms of service or gain an unfair advantage. The author is not responsible for bans, damage, misuse, or any legal consequences arising from the use of this software. Use it at your own risk.
